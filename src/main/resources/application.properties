#  Run server on port 8089
server.port=8089

#  MySQL DB Configuration (make sure DB name, user, password are correct)
spring.datasource.url=jdbc:mysql://localhost:3306/rest_db
spring.datasource.username=root
spring.datasource.password=Sachin@10

#  JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#  Optional: Logging SQL formatting
spring.jpa.properties.hibernate.format_sql=true

#  Optional: Enable dev tools (hot reload)
spring.devtools.restart.enabled=true
